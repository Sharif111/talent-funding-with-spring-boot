package com.user.app.repository;

import com.user.app.entity.SystemUserGroupMenuEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SystemUserGroupMenuRepo extends JpaRepository<SystemUserGroupMenuEntity, Long> {
}
